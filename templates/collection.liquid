<section>
    <h1>{{ meta.h1 }}</h1>
    <div class="{{ article.format }}">{{ collection.html }}</div>
    <div>
        {% for product in collection.products %}
    	    {% include 'productCard' %}
    	{% endfor %}
    </div>
</section>
